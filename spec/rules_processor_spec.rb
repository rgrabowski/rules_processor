require "spec_helper"

describe RulesProcessor do
  it "has a version number" do
    expect(RulesProcessor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
